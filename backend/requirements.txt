# ============================================================
# ASL Recognition PWA — Backend Dependencies
# Python 3.11+  |  Fixed & verified 2026-04-14
# ============================================================

# FastAPI and async web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database and ORM
sqlalchemy==2.0.31
asyncpg==0.29.0
alembic==1.13.1

# JWT and authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# Validation and serialization
pydantic==2.7.1
pydantic-settings==2.3.4
email-validator==2.2.0

# ----------------------------------------------------------
# ML STACK — DO NOT CHANGE THESE VERSIONS
#
# Both models (asl_model_full.keras, word_model.keras) were
# saved with Keras 3.13.2 on 2026-03-25 and 2026-04-13.
#
# - keras must be exactly 3.13.2 — older versions cannot
#   load .keras files saved by a newer minor version.
# - tensorflow 2.16+ is required for Keras 3.x support.
#   TF 2.15 bundles Keras 2.x internally and conflicts.
# - numpy 1.26.x is required by TF 2.16 + Keras 3.13.
#   numpy 1.24.x throws dtype and array API errors at
#   inference time.
# ----------------------------------------------------------
tensorflow==2.16.1
keras==3.13.2
numpy==1.26.4

# Production ASGI server
gunicorn==22.0.0

# Logging
python-json-logger==2.0.7

# Environment variables
python-dotenv==1.0.1

# ============================================================
# Development dependencies — uncomment to use locally
# ============================================================
# pytest==8.2.0
# pytest-asyncio==0.23.6
# pytest-cov==5.0.0
# httpx==0.27.0        ← required by pytest for async test client
# black==24.4.2
# flake8==7.0.0
# mypy==1.10.0